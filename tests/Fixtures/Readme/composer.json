{
    "name": "rdgs/pest-plugin-quality-readme-fixture",
    "description": "Throwaway project that runs the exact code samples published in the package README.",
    "type": "project",
    "repositories": [
        {
            "type": "path",
            "url": "../../..",
            "options": {
                "symlink": false
            }
        }
    ],
    "require": {
        "php": "^8.4"
    },
    "require-dev": {
        "ianrodrigues/pest-plugin-code-quality": "*",
        "pestphp/pest": "^5.0",
        "pestphp/pest-plugin-arch": "~5.0.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        },
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
